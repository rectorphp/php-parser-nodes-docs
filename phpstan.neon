parameters:
    level: max

    paths:
        - src
        - tests

    ignoreErrors:
        -
            message: '#Cannot cast (.*?) to string#'
            path: src/Command/DumpNodesCommand.php
